{
  "name": "ai-skills-registry",
  "version": "1.0.0",
  "private": true,
  "description": "AI Skills Registry + skillpool package manager",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:registry": "node scripts/build-registry.mjs",
    "build:cli": "npm run build --workspace skillpool",
    "build:cli-tui": "npm run build --workspace agentpool"
  },
  "license": "MIT"
}
