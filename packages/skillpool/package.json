{
  "name": "skillpool",
  "version": "2.1.0",
  "description": "Package manager for the AI Skills Registry — list, search, add, remove skills, commands, and agents for Claude Code",
  "type": "module",
  "bin": {
    "skillpool": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js"
  },
  "keywords": ["ai", "skills", "commands", "agents", "cli", "registry", "claude-code"],
  "license": "MIT",
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
