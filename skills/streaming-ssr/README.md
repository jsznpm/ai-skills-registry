# streaming-ssr

Teaches streaming server-side rendering for chunked HTML delivery. Use when you
need faster Time to First Byte and First Contentful Paint by streaming HTML as
it's generated on the server.

Install:
```bash
skillpool add streaming-ssr
```
