# upgrade-project

Upgrades npm dependencies in a project safely — checks outdated packages, bumps
versions, runs tests, and reports breaking changes.

Install:
```bash
skillpool add upgrade-project
```
